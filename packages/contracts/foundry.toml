[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

[rpc_endpoints]
op_sepolia = "https://sepolia.optimism.io"

[etherscan]
op_sepolia = { key = "${ETHERSCAN_API_KEY}", url = "https://api-sepolia-optimism.etherscan.io/api" }
